# Assets Instructions

Use this directory for static assets only.

Rules:

- Do not store generated source code, component files, or documentation here.
- Keep product assets named by purpose and stable enough for imports.
- Prefer shared icon registry entries for UI icons instead of adding one-off image assets.
