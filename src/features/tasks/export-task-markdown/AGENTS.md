# Export Task Markdown Feature Instructions

This feature owns task Markdown export formatting and export modal UI.

Rules:

- Keep Markdown serialization in `model`.
- Keep export modal layout in `components`.
- Do not put task list presentation or page route handling here.
- Do not import pages or widgets from this feature.
