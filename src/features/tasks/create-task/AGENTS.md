# Create Task Feature Instructions

This feature owns the create-task workflow UI and draft defaults.

Rules:

- Keep task creation form fields, create draft shape, and create-modal UI here.
- Do not own persistence or repositories here unless a dedicated feature action/hook is introduced.
- Reuse task entity metadata/UI for status and visibility display.
- Do not import pages or widgets from this feature.
