import { AdminClinicMetricsWorkspace } from '../../../features/admin-clinic-metrics'

export function AdminClinicMetricsPage({ routeParams = {}, runtime }) {
  return <AdminClinicMetricsWorkspace routeParams={routeParams} runtime={runtime} />
}
