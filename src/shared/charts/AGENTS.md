# Shared Charts Instructions

This directory owns generic chart rendering utilities.

Rules:

- Keep chart components data-shape oriented and domain-agnostic.
- Use semantic chart/theme tokens instead of hard-coded colors.
- Do not put dashboard-specific interpretation, copy, or business logic here.
- Dashboard-specific sections belong in widgets or features.
