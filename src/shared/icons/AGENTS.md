# Shared Icons Instructions

This directory owns the project icon registry.

Rules:

- Add common action/navigation/status icons here before using inline SVG in components.
- Keep icon names semantic and stable.
- Use entity metadata to choose domain status icons instead of hard-coding icons in each screen.
- Do not add decorative illustration logic here.
